{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "gcc build file",
            "command": "gcc",
            "args": [
                "-g",
                "${fileBasename}",
                // "-lComctl32",
                "-o",
                "${fileBasenameNoExtension}.exe"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}